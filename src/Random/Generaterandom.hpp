#ifndef GENERATERANDOM_HPP
#define GENERATERANDOM_HPP

#include <random>

unsigned int generaterandom(unsigned int,unsigned int); //!<generate a random number distribution (uniform) inbetween 2 unsigned integers (used to make random connections)

#endif 
